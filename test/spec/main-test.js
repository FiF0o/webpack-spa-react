import { expect } from 'chai'
import main from '../../src/main.js'

describe('An initialized app', () => {
  beforeEach(() => {
  })

  afterEach(() => {
  })

  it('Test something', () => {
  })
  xit('Skip this', (done) => {
  })
})
